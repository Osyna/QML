export enum CheckType {
  AI = 'ai',
  Keywords = 'keywords',
  Exact = 'exact',
  Manual = 'manual',
}

export enum AICheckType {
  Meaning = 'meaning',
  Sentiment = 'sentiment',
  Custom = 'custom',
}
